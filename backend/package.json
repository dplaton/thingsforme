{
  "name": "thingsforme-backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "prisma deploy",
    "dev": "nodemon -r dotenv/config",
    "debug": "nodemon --inspect -r dotenv/config"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^7.0.0",
    "graphql-yoga": "^1.17.4",
    "jsonwebtoken": "^9.0.0",
    "prisma-binding": "^2.3.9"
  }
}
